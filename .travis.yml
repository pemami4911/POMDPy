language: python
python:
  - "2.7"

addons:
  apt:
    packages:
      - libblas-dev
      - liblapack-dev
      - libatlas-base-dev
      - gfortran

install:
  - "pip install -r requirements.txt"
  - python setup.py install

script:
  - py.test test/unit_tests.py

